#include "monty.h"
/**
 * f_stack - A function that prints the top of a stack
 * @head: the head of a stack
 * @counter: number line
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
